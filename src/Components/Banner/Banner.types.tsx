export type BannerPropsTypes = {
    background: string,
    buttonShow?: boolean,
    buttonLink?: string,
    image?: string,
    badge?: string,
    BannerText: string,
    className?: string,
    Title?: string
}